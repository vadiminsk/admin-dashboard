import React from 'react';
import ReactDOM from 'react-dom';
import PrimarySearchAppBar from './components/App/index';

ReactDOM.render(
    <React.StrictMode>
        <PrimarySearchAppBar/>
    </React.StrictMode>,
    document.getElementById('root')
);
